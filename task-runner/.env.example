# .env.example
# The task runner needs its own environment variables, separate from the frontend.

# Supabase Connection
VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# AI Provider Keys
# Note: In CrewForm, we primarily use the user/workspace API keys from the database (BYOK pattern).
# However, if your agent configurations fall back to environment variables, provide them here.
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_GENERATIVE_AI_API_KEY=""

# Encryption Key for AES-256-GCM (must match the Edge Function)
ENCRYPTION_KEY="YOUR_32_BYTE_HEX_STRING_FOR_DECRYPTION"
